<?xml version="1.0" encoding="UTF-8"?>

<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.oracle.nosql.sdk</groupId>
  <artifactId>nosql-java-sdk</artifactId>
  <version>5.2.23-SNAPSHOT</version>
  <packaging>pom</packaging>
  <name>nosql-java-sdk</name>
  <url>https://github.com/oracle/nosql-java-sdk</url>

  <modules>
    <module>driver</module>
    <module>examples</module>
  </modules>

  <profiles>
    <!-- skip tests -->
    <profile>
      <id>xtest</id>
      <properties>
        <maven.test.skip>true</maven.test.skip>
      </properties>
    </profile>

    <!-- skip javadoc -->
    <profile>
      <id>xdoc</id>
      <properties>
        <maven.javadoc.skip>true</maven.javadoc.skip>
      </properties>
    </profile>
  </profiles>

  <!-- INSERT DISTRIBUTION MANAGEMENT -->

  <build>
    <defaultGoal>clean compile</defaultGoal>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <configuration>
            <source>1.8</source>
            <target>1.8</target>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-source-plugin</artifactId>
          <version>3.2.0</version>
          <executions>
            <execution>
              <id>attach-sources</id>
              <goals>
                <goal>jar</goal>
              </goals>
            </execution>
          </executions>
        </plugin>

        <!-- javadoc -->
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-javadoc-plugin</artifactId>
          <version>3.2.0</version>
          <configuration>
            <source>1.8</source>
            <!-- exclude these packages.
                 TODO: the this list should include
                 oracle.nosql.driver.ops.serde. The problem is that if added
                 javadoc fails as it tries to operate on files in that package anyway.
                 May need to post-process the doc to remove the package and link.
            -->
            <excludePackageNames>oracle.nosql.driver.http;oracle.nosql.driver.httpclient;oracle.nosql.driver.query;oracle.nosql.driver.util</excludePackageNames>
            <stylesheetfile>${basedir}/../doc/style.css</stylesheetfile>
            <show>public</show>
            <header><![CDATA[<b>Oracle NoSQL Database</b><br><font size=\"-1\"> Version ${project.version} ${maven.build.timestamp}</font>]]></header>
            <bottom><![CDATA[<font size=1>${copyright}</font>]]> </bottom>
            <link>${java.apidoc}</link>
            <additionalOptions>
              <additionalOption>-author</additionalOption>
            </additionalOptions>
            <!-- put output in target/apidocs -->
            <reportOutputDirectory>
              ${project.build.directory}/apidocs
            </reportOutputDirectory>
          </configuration>
          <executions>
            <execution>
              <id>javadoc-jar</id>
              <goals>
                <goal>jar</goal>
                <goal>javadoc</goal>
              </goals>
              <phase>package</phase>
            </execution>
          </executions>
        </plugin>

        <plugin>
          <artifactId>maven-assembly-plugin</artifactId>
          <version>3.1.1</version>
          <configuration>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.codehaus.mojo</groupId>
          <artifactId>versions-maven-plugin</artifactId>
          <version>2.7</version>
          <configuration>
            <autoVersionSubmodules>true</autoVersionSubmodules>
          </configuration>
        </plugin>
      </plugins>
    </pluginManagement>
  </build>
</project>
